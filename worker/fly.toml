app = "bv-zk-worker"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  ZK_RESULTS_BUCKET = "zk-results"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
